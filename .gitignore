/.phpunit.cache
/bootstrap/ssr
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/storage/dashboard-analysis
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder
/vendor
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
implementation_plan.md
plan_docs
.codegraph
junit.xml
tsc-output.txt
.phpunit.clean-db.xml
.quality-promote-ok
# phpstan.neon's tmpDir (repo-local result cache) + the composer.lock fingerprint
# bin/ci-dependency-integrity-lint.php invalidates it against.
/build

# Claude Code — PER-USER / runtime state, never shared.
# settings.local.json holds one machine's permission allowlist (it has carried
# absolute /Users/<name>/... paths) and conflicted on every cross-machine merge;
# scheduled_tasks.lock is runtime state. Shared Claude config (.claude/settings.json,
# agents/, skills/, commands/) stays tracked — only these two are ignored.
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local drive-fixture environment (secrets copied from .env — never commit)
.env.drive
