resources/js/components/ui/*
resources/views/mail/*

# Generated by `rbac:derive-access` / `rbac:derive-map`, never hand-edited. Prettier and the
# generator disagree on formatting, so reformatting them only guarantees that the next regeneration
# reverts it — and these files are resolved by regenerating, never by editing a diff.
tests/fixtures/*.json
